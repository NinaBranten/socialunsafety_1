---
title: "Frequencies"
#bibliography: references.bib
author: "Nina Branten"
---


```{r, globalsettings, echo=FALSE, warning=FALSE, results='hide'}
library(knitr)

knitr::opts_chunk$set(echo = TRUE)
opts_chunk$set(tidy.opts=list(width.cutoff=100),tidy=TRUE, warning = FALSE, message = FALSE,comment = "#>", cache=TRUE, class.source=c("test"), class.output=c("test2"))
options(width = 100)
rgl::setupKnitr()



colorize <- function(x, color) {sprintf("<span style='color: %s;'>%s</span>", color, x) }

```

```{r klippy1, echo=FALSE, include=TRUE}
klippy::klippy(position = c('top', 'right'))
#klippy::klippy(color = 'darkred')
#klippy::klippy(tooltip_message = 'Click to copy', tooltip_success = 'Done')
```

```{r echo=FALSE, warning=FALSE}
library("foreign")
library("ggplot2")
library("summarytools")
```

```{r klippy2, echo=FALSE, include=TRUE, results='hide', warning=FALSE, message=FALSE}
NEA2022 <-read.spss("data/processed/Data 2022 cleaned.sav", use.value.labels = T, to.data.frame = T)

#NEA20142022 <-read.spss("data/raw/Data 2014-2022.sav", use.value.labels = T, to.data.frame = T)
```

Pesten door collega's

```{r}
summarytools::freq(NEA2022$afl_pe_1, order = "freq")

```
Pesten door leidinggevenden

```{r}
summarytools::freq(NEA2022$afl_pe_2, order = "freq")
```
Intimidatie door collega's

```{r}
summarytools::freq(NEA2022$afl_be_1, order = "freq")
```
Intimidatie door leidinggevenden

```{r}
summarytools::freq(NEA2022$afl_be_2, order = "freq")
```

Fysiek geweld door collega's 

```{r}
summarytools::freq(NEA2022$afl_ge_3, order = "freq")
```

Fysiek geweld door leidinggevenden

```{r}
summarytools::freq(NEA2022$afl_ge_4, order = "freq")
```

Afgelopen maanden te maken gehad met intern ongewenst gedrag: dus op een van de bovenstaande vragen 'een enkele keer' of vaker.

```{r}
summarytools::freq(NEA2022$afl_on_3, order = "freq")
```
Conflict met collega

```{r}
summarytools::freq(NEA2022$conflict, order = "freq")
```
Conflict met directe leidinggevende

```{r}
summarytools::freq(NEA2022$confli_1, order = "freq")
```

Conflict met uw werkgever

```{r}
summarytools::freq(NEA2022$confli_2, order = "freq")
```
Kort- en/of langdurig conflict met collega's, leidinggevende en/of werkgever

```{r}
summary(NEA2022$afl_conf)
```
Sociale steun leidinggevenden

```{r}
summarytools::freq(NEA2022$afl_socs, order = "freq")
```
Sociale steun leidinggevenden: mijn leidinggevende heeft oog voor het welzijn van de medewerkers
```{r}
summarytools::freq(NEA2022$leiding_, order = "freq")
```
Sociale steun leidinggevenden: mijn leidinggevende besteedt aandacht aan wat ik zeg
```{r}
summarytools::freq(NEA2022$leidin_1, order = "freq")
```

Sociale steun collega's

```{r}
summarytools::freq(NEA2022$afl_so_2, order = "freq")
```

Sociale steun collega's: mijn collega's hebben persoonlijke belangstelling voor me

```{r}
summarytools::freq(NEA2022$collega_, order = "freq")
```

Sociale steun collega's: mijn collega's zijn vriendelijk

```{r}
summarytools::freq(NEA2022$colleg_1, order = "freq")
```

Psychologische veiligheid: op mijn werk kun je lastige kwesties naar voren brengen

```{r}
summarytools::freq(NEA2022$psyveil, order = "freq")
```

Psychologische veiligheid: op mijn werk ik het gemakkelijk om hulp te vragen

```{r}
summarytools::freq(NEA2022$psyvei_1, order = "freq")
```

Psychologische veiligheid: op mijn werk is het niet erg om een fout te maken

```{r}
summarytools::freq(NEA2022$psyvei_2, order = "freq")
```